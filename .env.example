# Ejemplo de archivo .env para Sakorms Inventory
# Copia este archivo a '.env' y rellena los valores para pruebas locales.

# Host o nombre del servidor SQL (puede incluir instancia o IP)
DB_SERVER=sakorms
# Puerto opcional (ej: 1433)
DB_PORT=1433
DB_DATABASE=inventory

# Si usas SQL Authentication para pruebas locales, establece estas variables.
# Si prefieres Integrated Security (Windows Auth), deja DB_UID y DB_PWD en vacío.
DB_UID=
DB_PWD=

# Alternativa: si tu entorno impide Windows Auth con Apache, puedes establecer credenciales temporales aquí
# DB_ALT_UID=sa
# DB_ALT_PWD=YourStrong!Passw0rd

# Opciones de cifrado - true|false
DB_ENCRYPT=true
DB_TRUST_SERVER_CERTIFICATE=false
DB_CONNECTION_TIMEOUT=30
DB_APP_NAME="Sakorms Inventory"
